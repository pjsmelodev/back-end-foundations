VIDEO QUESTIONS:
Which of the following options correctly describes the purpose of an if-else statement?
    It allows the program to perform different actions based on whether a condition is true or false

Which of the following options correctly describes the purpose of a switch statement?
    It allows a program to choose between multiple options based on the value of a variable

Which control structure would be most appropriate for providing different workout plans based on the user’s level (beginner, intermediate, advanced)?
    Switch statements

GUIDED LAB:
int age;

Console.WriteLine("Enter your age: ");

age = int.Parse(Console.ReadLine());

if (age >= 18) {
    Console.WriteLine("You are aligible to vote.");
}
else {
    Console.WriteLine("You are not eligible to vote.");
}

PRACTICE QUIZ:
1. What is the primary purpose of using an if-else statement in programming?
    To choose actions based on a condition

2. Which scenario is most suitable for using a switch statement instead of an if-else statement?
    When choosing actions based on multiple values

3. You are developing a program that assigns grades based on a student's score. Which control structure is more efficient for handling multiple grade ranges (A, B, C, D, F)?
    Use a switch statement to handle grade ranges